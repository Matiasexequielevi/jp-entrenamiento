<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login - JP Entrenamiento</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    body {
      background-color: #e6eaf3;
      display: flex;
      flex-direction: column;
      min-height: 100vh;
      position: relative;
      overflow: hidden;
    }

    /* ✅ Marca de agua del logo */
    body::before {
      content: "";
      position: absolute;
      top: 50%;
      left: 50%;
      width: 500px;
      height: 500px;
      background: url('/images/logo-jp.png') no-repeat center center;
      background-size: contain;
      opacity: 0.06;
      transform: translate(-50%, -50%);
      z-index: 0;
    }

    .login-container {
      flex: 1;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      padding: 2rem;
      position: relative;
      z-index: 1;
    }

    .login-logo {
      width: 160px;
      margin-bottom: 1.5rem;
      animation: girarExpandir 1.8s ease-in-out forwards;
      opacity: 0;
      transition: all 0.5s ease;
    }

    .login-logo:hover {
      transform: rotateY(360deg) scale(1.2);
      filter: brightness(1.8) drop-shadow(0 0 10px #6c63ff);
    }

    @keyframes girarExpandir {
      0% {
        transform: rotateY(0deg) scale(0.5);
        opacity: 0;
        filter: brightness(0.5);
      }
      50% {
        transform: rotateY(180deg) scale(1.2);
        opacity: 0.6;
        filter: brightness(1.5);
      }
      100% {
        transform: rotateY(360deg) scale(1);
        opacity: 1;
        filter: brightness(1);
      }
    }

    .login-box {
      background: white;
      padding: 2rem;
      border-radius: 16px;
      box-shadow: 0 8px 25px rgba(0, 0, 0, 0.15);
      width: 100%;
      max-width: 400px;
    }

    .login-box .form-label {
      font-weight: 500;
      color: #3b3b3b;
    }

    .btn-primary {
      background-color: #4455aa;
      border-color: #4455aa;
    }

    .btn-primary:hover {
      background-color: #2c3e95;
      border-color: #2c3e95;
    }

    footer {
      text-align: center;
      padding: 1rem;
      background-color: #b2bcdd;
      color: #302d2d;
      font-size: 0.9rem;
    }
  </style>
</head>
<body>

  <div class="login-container">
    <img src="/images/logo-jp.png" alt="Logo JP" class="login-logo">
    <div class="login-box">
      <form action="/login" method="POST">
        <div class="mb-3">
          <label for="usuario" class="form-label">Usuario</label>
          <input type="text" class="form-control" id="usuario" name="usuario" required>
        </div>
        <div class="mb-3">
          <label for="contrasena" class="form-label">Contraseña</label>
          <input type="password" class="form-control" id="contrasena" name="contrasena" required>
        </div>
        <div class="d-grid">
          <button type="submit" class="btn btn-primary">Ingresar</button>
        </div>
      </form>
    </div>
  </div>

  <footer>
    Todos los derechos reservados - Levi Prog.
  </footer>

</body>
</html>
